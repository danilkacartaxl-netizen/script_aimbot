-- ==============================================
-- WINSTIK NEO v3.0 (Ultra Compact)
-- Размер: 220x310. Черный минимализм. 
-- Функции: Aimbot (Smooth + Head/Torso), ESP (Skeleton/Torso/Box + Wallhack)
-- ==============================================

local player = game.Players.LocalPlayer
local camera = workspace.CurrentCamera
local mouse = player:GetMouse()
local runService = game:GetService("RunService")
local userInput = game:GetService("UserInputService")
local players = game:GetService("Players")

-- ==============================================
-- НАСТРОЙКИ ПО УМОЛЧАНИЮ
-- ==============================================
local Settings = {
    aimbot = false,
    esp = false,
    wallhack = false,
    espStyle = "Skeleton", -- "Skeleton", "Torso", "Box"
    aimPart = "Head",      -- "Head", "Torso"
    fov = 150,
    smooth = 0.25,
    espColor = Color3.fromRGB(139, 92, 246) -- Фиолетовый
}
local espObjects = {}
local fovCircleDraw = nil

-- ==============================================
-- СОЗДАНИЕ GUI (ОЧЕНЬ МАЛЕНЬКОЕ, ЧЕРНОЕ)
-- ==============================================
local gui = Instance.new("ScreenGui")
gui.Name = "WINSTIK_NEO"
gui.ResetOnSpawn = false
gui.Parent = player.PlayerGui

-- Кнопка открытия (D)
local btn = Instance.new("TextButton")
btn.Size = UDim2.new(0, 32, 0, 32)
btn.Position = UDim2.new(0, 20, 0, 20)
btn.BackgroundColor3 = Color3.fromRGB(8, 8, 10)
btn.TextColor3 = Color3.fromRGB(255, 255, 255)
btn.Text = "D"
btn.Font = Enum.Font.GothamBold
btn.TextSize = 20
btn.BorderSizePixel = 0
btn.Parent = gui

local btnCorner = Instance.new("UICorner")
btnCorner.CornerRadius = UDim.new(0, 8)
btnCorner.Parent = btn

local btnStroke = Instance.new("UIStroke")
btnStroke.Color = Settings.espColor
btnStroke.Thickness = 1.5
btnStroke.Transparency = 0.8
btnStroke.Parent = btn

-- Главное меню (220x310)
local menu = Instance.new("Frame")
menu.Size = UDim2.new(0, 220, 0, 310)
menu.BackgroundColor3 = Color3.fromRGB(5, 5, 5)
menu.BackgroundTransparency = 0.1
menu.BorderSizePixel = 0
menu.Visible = false
menu.Parent = gui

local menuCorner = Instance.new("UICorner")
menuCorner.CornerRadius = UDim.new(0, 10)
menuCorner.Parent = menu

local menuStroke = Instance.new("UIStroke")
menuStroke.Color = Color3.fromRGB(40, 40, 40)
menuStroke.Thickness = 1
menuStroke.Parent = menu

-- Заголовок
local title = Instance.new("TextLabel")
title.Size = UDim2.new(1, -15, 0, 30)
title.Position = UDim2.new(0, 10, 0, 10)
title.BackgroundTransparency = 1
title.Text = "WINSTIK"
title.TextColor3 = Color3.fromRGB(255, 255, 255)
title.Font = Enum.Font.GothamBold
title.TextSize = 16
title.TextXAlignment = Enum.TextXAlignment.Left
title.Parent = menu

local titleAccent = Instance.new("TextLabel")
titleAccent.Size = UDim2.new(0, 15, 0, 30)
titleAccent.Position = UDim2.new(0, 90, 0, 10)
titleAccent.BackgroundTransparency = 1
titleAccent.Text = "$"
titleAccent.TextColor3 = Settings.espColor
titleAccent.Font = Enum.Font.GothamBold
titleAccent.TextSize = 16
titleAccent.TextXAlignment = Enum.TextXAlignment.Left
titleAccent.Parent = menu

-- Линия разделителя
local line = Instance.new("Frame")
line.Size = UDim2.new(0.9, 0, 0, 1)
line.Position = UDim2.new(0.05, 0, 0, 42)
line.BackgroundColor3 = Settings.espColor
line.BackgroundTransparency = 0.8
line.BorderSizePixel = 0
line.Parent = menu

-- ==============================================
-- КОМПОНЕНТЫ UI (ТОГГЛЫ, ЧОЙСЫ, СЛАЙДЕРЫ)
-- ==============================================
local function createFrame(yPos)
    local f = Instance.new("Frame")
    f.Size = UDim2.new(0.9, 0, 0, 26)
    f.Position = UDim2.new(0.05, 0, 0, yPos)
    f.BackgroundColor3 = Color3.fromRGB(15, 15, 15)
    f.BorderSizePixel = 0
    f.Parent = menu
    return f
end

local function createToggle(yPos, text, getter, setter)
    local f = createFrame(yPos)
    local l = Instance.new("TextLabel")
    l.Size = UDim2.new(0.6, 0, 1, 0)
    l.Position = UDim2.new(0.05, 0, 0, 0)
    l.BackgroundTransparency = 1
    l.Text = text
    l.TextColor3 = Color3.fromRGB(220, 220, 220)
    l.Font = Enum.Font.Gotham
    l.TextSize = 12
    l.TextXAlignment = Enum.TextXAlignment.Left
    l.Parent = f

    local b = Instance.new("TextButton")
    b.Size = UDim2.new(0, 30, 0, 16)
    b.Position = UDim2.new(1, -30, 0.5, -8)
    b.BackgroundColor3 = Color3.fromRGB(50, 50, 50)
    b.Text = ""
    b.BorderSizePixel = 0
    b.Parent = f

    local bCorner = Instance.new("UICorner")
    bCorner.CornerRadius = UDim.new(1, 0)
    bCorner.Parent = b

    local ind = Instance.new("Frame")
    ind.Size = UDim2.new(0, 12, 0, 12)
    ind.Position = UDim2.new(0, 2, 0.5, -6)
    ind.BackgroundColor3 = Color3.fromRGB(200, 200, 200)
    ind.BorderSizePixel = 0
    ind.Parent = b

    local indCorner = Instance.new("UICorner")
    indCorner.CornerRadius = UDim.new(1, 0)
    indCorner.Parent = ind

    b.MouseButton1Click:Connect(function()
        local newState = not getter()
        setter(newState)
        if newState then
            b.BackgroundColor3 = Settings.espColor
            ind.Position = UDim2.new(1, -14, 0.5, -6)
            ind.BackgroundColor3 = Color3.fromRGB(255, 255, 255)
        else
            b.BackgroundColor3 = Color3.fromRGB(50, 50, 50)
            ind.Position = UDim2.new(0, 2, 0.5, -6)
            ind.BackgroundColor3 = Color3.fromRGB(200, 200, 200)
        end
    end)
end

local function createChoice(yPos, text, options, getter, setter)
    local f = createFrame(yPos)
    local l = Instance.new("TextLabel")
    l.Size = UDim2.new(0.6, 0, 1, 0)
    l.Position = UDim2.new(0.05, 0, 0, 0)
    l.BackgroundTransparency = 1
    l.Text = text
    l.TextColor3 = Color3.fromRGB(220, 220, 220)
    l.Font = Enum.Font.Gotham
    l.TextSize = 12
    l.TextXAlignment = Enum.TextXAlignment.Left
    l.Parent = f

    local b = Instance.new("TextButton")
    b.Size = UDim2.new(0, 50, 0, 20)
    b.Position = UDim2.new(1, -50, 0.5, -10)
    b.BackgroundColor3 = Color3.fromRGB(20, 20, 20)
    b.Text = getter()
    b.TextColor3 = Color3.fromRGB(255, 255, 255)
    b.Font = Enum.Font.GothamBold
    b.TextSize = 10
    b.BorderSizePixel = 0
    b.Parent = f

    local bCorner = Instance.new("UICorner")
    bCorner.CornerRadius = UDim.new(0, 4)
    bCorner.Parent = b

    b.MouseButton1Click:Connect(function()
        local current = getter()
        local idx = table.find(options, current) or 1
        local newIdx = idx % #options + 1
        setter(options[newIdx])
        b.Text = options[newIdx]
    end)
end

-- ==============================================
-- ЗАПОЛНЕНИЕ МЕНЮ
-- ==============================================
createToggle(54, "Aimbot", function() return Settings.aimbot end, function(v) Settings.aimbot = v end)
createToggle(88, "ESP", function() return Settings.esp end, function(v) Settings.esp = v end)
createToggle(122, "Wallhack", function() return Settings.wallhack end, function(v) Settings.wallhack = v end)

createChoice(156, "ESP Style", {"Skeleton", "Torso", "Box"}, 
    function() return Settings.espStyle end, 
    function(v) Settings.espStyle = v end)

createChoice(190, "Aim Part", {"Head", "Torso"}, 
    function() return Settings.aimPart end, 
    function(v) Settings.aimPart = v end)

-- FOV Ползунок
local fovFrame = createFrame(224)
fovFrame.Size = UDim2.new(0.9, 0, 0, 30)

local fovLabel = Instance.new("TextLabel")
fovLabel.Size = UDim2.new(0.6, 0, 1, 0)
fovLabel.Position = UDim2.new(0.05, 0, 0, 0)
fovLabel.BackgroundTransparency = 1
fovLabel.Text = "FOV: " .. Settings.fov
fovLabel.TextColor3 = Color3.fromRGB(220, 220, 220)
fovLabel.Font = Enum.Font.Gotham
fovLabel.TextSize = 11
fovLabel.TextXAlignment = Enum.TextXAlignment.Left
fovLabel.Parent = fovFrame

local fovBox = Instance.new("TextBox")
fovBox.Size = UDim2.new(0, 40, 0, 20)
fovBox.Position = UDim2.new(1, -40, 0.5, -10)
fovBox.BackgroundColor3 = Color3.fromRGB(20, 20, 20)
fovBox.TextColor3 = Color3.fromRGB(255, 255, 255)
fovBox.Text = tostring(Settings.fov)
fovBox.Font = Enum.Font.GothamBold
fovBox.TextSize = 11
fovBox.BorderSizePixel = 0
fovBox.Parent = fovFrame

local fBoxCorner = Instance.new("UICorner")
fBoxCorner.CornerRadius = UDim.new(0, 4)
fBoxCorner.Parent = fovBox

fovBox.FocusLost:Connect(function()
    local v = tonumber(fovBox.Text)
    if v then
        Settings.fov = math.clamp(v, 50, 500)
        fovLabel.Text = "FOV: " .. Settings.fov
        fovBox.Text = tostring(Settings.fov)
    else
        fovBox.Text = tostring(Settings.fov)
    end
end)

-- Smooth Ползунок
local smoothFrame = createFrame(262)
smoothFrame.Size = UDim2.new(0.9, 0, 0, 30)

local smoothLabel = Instance.new("TextLabel")
smoothLabel.Size = UDim2.new(0.6, 0, 1, 0)
smoothLabel.Position = UDim2.new(0.05, 0, 0, 0)
smoothLabel.BackgroundTransparency = 1
smoothLabel.Text = "Smooth: " .. string.format("%.2f", Settings.smooth)
smoothLabel.TextColor3 = Color3.fromRGB(220, 220, 220)
smoothLabel.Font = Enum.Font.Gotham
smoothLabel.TextSize = 11
smoothLabel.TextXAlignment = Enum.TextXAlignment.Left
smoothLabel.Parent = smoothFrame

local smoothBox = Instance.new("TextBox")
smoothBox.Size = UDim2.new(0, 40, 0, 20)
smoothBox.Position = UDim2.new(1, -40, 0.5, -10)
smoothBox.BackgroundColor3 = Color3.fromRGB(20, 20, 20)
smoothBox.TextColor3 = Color3.fromRGB(255, 255, 255)
smoothBox.Text = string.format("%.2f", Settings.smooth)
smoothBox.Font = Enum.Font.GothamBold
smoothBox.TextSize = 11
smoothBox.BorderSizePixel = 0
smoothBox.Parent = smoothFrame

local sBoxCorner = Instance.new("UICorner")
sBoxCorner.CornerRadius = UDim.new(0, 4)
sBoxCorner.Parent = smoothBox

smoothBox.FocusLost:Connect(function()
    local v = tonumber(smoothBox.Text)
    if v then
        Settings.smooth = math.clamp(v, 0.05, 0.8)
        smoothLabel.Text = "Smooth: " .. string.format("%.2f", Settings.smooth)
        smoothBox.Text = string.format("%.2f", Settings.smooth)
    else
        smoothBox.Text = string.format("%.2f", Settings.smooth)
    end
end)

-- ==============================================
-- ПЕРЕТАСКИВАНИЕ ОКНА (BDEV СТИЛЬ)
-- ==============================================
local dragging = false
local dragStart, startPos

btn.InputBegan:Connect(function(input)
    if input.UserInputType == Enum.UserInputType.MouseButton1 then
        dragging = true
        dragStart = input.Position
        startPos = btn.Position
        userInput.MouseIconEnabled = false
    end
end)

btn.InputEnded:Connect(function(input)
    if input.UserInputType == Enum.UserInputType.MouseButton1 and dragging then
        dragging = false
        userInput.MouseIconEnabled = true
    end
end)

userInput.InputChanged:Connect(function(input)
    if dragging and input.UserInputType == Enum.UserInputType.MouseMovement then
        local delta = input.Position - dragStart
        btn.Position = UDim2.new(startPos.X.Scale, startPos.X.Offset + delta.X, startPos.Y.Scale, startPos.Y.Offset + delta.Y)
        
        if menu.Visible then
            local absX = btn.AbsolutePosition.X
            local screenW = camera.ViewportSize.X
            if absX < screenW / 2 then
                menu.Position = UDim2.new(0, btn.AbsolutePosition.X + 40, 0, btn.AbsolutePosition.Y - 10)
            else
                menu.Position = UDim2.new(0, btn.AbsolutePosition.X - 225, 0, btn.AbsolutePosition.Y - 10)
            end
        end
    end
end)

-- ==============================================
-- ОТКРЫТИЕ/ЗАКРЫТИЕ (B ИЛИ D)
-- ==============================================
btn.MouseButton1Click:Connect(function()
    menu.Visible = not menu.Visible
    if menu.Visible then
        local absX = btn.AbsolutePosition.X
        local screenW = camera.ViewportSize.X
        if absX < screenW / 2 then
            menu.Position = UDim2.new(0, btn.AbsolutePosition.X + 40, 0, btn.AbsolutePosition.Y - 10)
        else
            menu.Position = UDim2.new(0, btn.AbsolutePosition.X - 225, 0, btn.AbsolutePosition.Y - 10)
        end
    end
end)

userInput.InputBegan:Connect(function(input)
    if input.KeyCode == Enum.KeyCode.B or input.KeyCode == Enum.KeyCode.D then
        btn.MouseButton1Click:Fire()
    end
end)

-- ==============================================
-- КРУГ FOV
-- ==============================================
local fovCircle = Instance.new("Frame")
fovCircle.Name = "FOVCircle"
fovCircle.BackgroundTransparency = 1
fovCircle.BorderSizePixel = 0
fovCircle.AnchorPoint = Vector2.new(0.5, 0.5)
fovCircle.Position = UDim2.new(0.5, 0, 0.5, 0)
fovCircle.Size = UDim2.new(0, 1, 0, 1)
fovCircle.Visible = false
fovCircle.Parent = gui

local circleUI = Instance.new("UICorner")
circleUI.CornerRadius = UDim.new(1, 0)
circleUI.Parent = fovCircle

local circleStroke = Instance.new("UIStroke")
circleStroke.Color = Settings.espColor
circleStroke.Thickness = 2
circleStroke.Transparency = 0.3
circleStroke.Parent = fovCircle

-- ==============================================
-- ЛОГИКА ESP (СКЕЛЕТ, БОКС, ТОРС, ИМЯ, ХП)
-- ==============================================
local function createESP(plr)
    if espObjects[plr] or not plr.Character then return end
    local char = plr.Character
    local folder = Instance.new("Folder")
    folder.Name = "NEO_ESP"
    folder.Parent = char

    -- Отрисовка выбранного стиля
    if Settings.espStyle == "Box" then
        local box = Instance.new("BoxHandleAdornment")
        box.Size = Vector3.new(3.5, 5.5, 1.5)
        box.Adornee = char
        box.Color3 = Settings.espColor
        box.Transparency = 0.2
        box.AlwaysOnTop = Settings.wallhack
        box.Parent = folder
    elseif Settings.espStyle == "Torso" then
        local torso = char:FindFirstChild("UpperTorso") or char:FindFirstChild("Torso")
        if torso then
            local box = Instance.new("BoxHandleAdornment")
            box.Size = Vector3.new(2.5, 3, 1.5)
            box.Adornee = torso
            box.Color3 = Settings.espColor
            box.Transparency = 0.2
            box.AlwaysOnTop = Settings.wallhack
            box.Parent = folder
        end
    elseif Settings.espStyle == "Skeleton" then
        local function addLine(p1, p2)
            local line = Instance.new("LineHandleAdornment")
            line.Adornee = char
            line.Point1 = p1.Position
            line.Point2 = p2.Position
            line.Color3 = Settings.espColor
            line.Thickness = 1.5
            line.Transparency = 0.2
            line.AlwaysOnTop = Settings.wallhack
            line.Parent = folder
        end
        local head = char:FindFirstChild("Head")
        local torso = char:FindFirstChild("UpperTorso") or char:FindFirstChild("Torso")
        local la = char:FindFirstChild("Left Arm") or char:FindFirstChild("LeftUpperArm")
        local ra = char:FindFirstChild("Right Arm") or char:FindFirstChild("RightUpperArm")
        local ll = char:FindFirstChild("Left Leg") or char:FindFirstChild("LeftLowerLeg")
        local rl = char:FindFirstChild("Right Leg") or char:FindFirstChild("RightLowerLeg")
        if head and torso then
            addLine(head, torso)
            if la then addLine(torso, la) end
            if ra then addLine(torso, ra) end
            if ll then addLine(torso, ll) end
            if rl then addLine(torso, rl) end
        end
    end

    -- Имя
    local nameGui = Instance.new("BillboardGui")
    nameGui.Adornee = char:FindFirstChild("Head")
    nameGui.Size = UDim2.new(0, 100, 0, 18)
    nameGui.StudsOffset = Vector3.new(0, 2.5, 0)
    nameGui.Parent = folder
    local nameLabel = Instance.new("TextLabel")
    nameLabel.Size = UDim2.new(1, 0, 1, 0)
    nameLabel.BackgroundTransparency = 1
    nameLabel.Text = plr.Name
    nameLabel.TextColor3 = Color3.fromRGB(255, 255, 255)
    nameLabel.TextScaled = true
    nameLabel.Font = Enum.Font.GothamBold
    nameLabel.Parent = nameGui

    -- Здоровье
    local healthGui = Instance.new("BillboardGui")
    healthGui.Adornee = char:FindFirstChild("Head")
    healthGui.Size = UDim2.new(0, 50, 0, 4)
    healthGui.StudsOffset = Vector3.new(0, 2, 0)
    healthGui.Parent = folder
    local bg = Instance.new("Frame")
    bg.Size = UDim2.new(1, 0, 1, 0)
    bg.BackgroundColor3 = Color3.fromRGB(30, 30, 30)
    bg.Parent = healthGui
    local fill = Instance.new("Frame")
    fill.BackgroundColor3 = Color3.fromRGB(0, 255, 0)
    fill.Size = UDim2.new(1, 0, 1, 0)
    fill.Parent = bg

    espObjects[plr] = {Folder = folder, Health = fill}
end

-- ==============================================
-- AIMBOT (С ВЫБОРОМ ЧАСТИ ТЕЛА И SMOOTH)
-- ==============================================
local function getClosestPlayer()
    local closest, shortest = nil, Settings.fov
    local center = Vector2.new(camera.ViewportSize.X / 2, camera.ViewportSize.Y / 2)
    
    for _, plr in ipairs(players:GetPlayers()) do
        if plr ~= player and plr.Character and plr.Character:FindFirstChild("Humanoid") and plr.Character.Humanoid.Health > 0 then
            local part = plr.Character:FindFirstChild(Settings.aimPart == "Head" and "Head" or "UpperTorso") 
                         or plr.Character:FindFirstChild(Settings.aimPart == "Head" and "Head" or "Torso")
            if part then
                local pos, onScreen = camera:WorldToViewportPoint(part.Position)
                if onScreen and pos.Z > 0 then
                    local dist = (Vector2.new(pos.X, pos.Y) - center).Magnitude
                    if dist < shortest then
                        if Settings.wallhack then
                            local camPos = camera.CFrame.Position
                            local ray = Ray.new(camPos, part.Position - camPos)
                            local hit = workspace:FindPartOnRayWithIgnoreList(ray, {player.Character})
                            if hit then 
                                -- Продолжаем поиск, если стена мешает (Wallhack отключает проверку на самом деле)
                            end
                        end
                        shortest = dist
                        closest = plr
                    end
                end
            end
        end
    end
    return closest
end

-- ==============================================
-- ОСНОВНОЙ ЦИКЛ (RENDER STEPPED)
-- ==============================================
runService.RenderStepped:Connect(function()
    -- Обновление круга FOV
    local circleSize = Settings.fov * 2
    fovCircle.Size = UDim2.new(0, circleSize, 0, circleSize)
    fovCircle.Visible = Settings.aimbot
    
    -- ESP
    if Settings.esp then
        for _, plr in ipairs(players:GetPlayers()) do
            if plr ~= player and plr.Character and not espObjects[plr] then
                createESP(plr)
            end
        end
        for plr, data in pairs(espObjects) do
            if not plr.Character or not plr.Character.Parent then
                data.Folder:Destroy()
                espObjects[plr] = nil
            else
                local hum = plr.Character:FindFirstChild("Humanoid")
                if hum then
                    data.Health.Size = UDim2.new(hum.Health / hum.MaxHealth, 0, 1, 0)
                end
            end
        end
    else
        for _, data in pairs(espObjects) do
            data.Folder:Destroy()
        end
        espObjects = {}
    end

    -- Aimbot (Плавный)
    if Settings.aimbot then
        local target = getClosestPlayer()
        if target then
            local aimPartObj = target.Character:FindFirstChild(Settings.aimPart == "Head" and "Head" or "UpperTorso") 
                             or target.Character:FindFirstChild(Settings.aimPart == "Head" and "Head" or "Torso")
            if aimPartObj then
                local aimPos = aimPartObj.Position
                local camPos = camera.CFrame.Position
                local direction = (aimPos - camPos).Unit
                camera.CFrame = camera.CFrame:Lerp(CFrame.new(camPos, camPos + direction), Settings.smooth)
            end
        end
    end
end)

print("WINSTIK NEO v3.0 Loaded! Press B or D to open.")
