-- ==============================================
-- SWILL Light (ESP + Aimbot + Wall + Hitboxes)
-- Menu: B или D
-- Чёрно-красный, компактный
-- ==============================================

local plr = game.Players.LocalPlayer
local ws = workspace
local cam = ws.CurrentCamera
local rs = game:GetService("RunService")
local uis = game:GetService("UserInputService")
local players = game:GetService("Players")

local gui = Instance.new("ScreenGui")
gui.Name = "SWILL_Light"
gui.ResetOnSpawn = false
gui.ZIndexBehavior = Enum.ZIndexBehavior.Sibling
gui.Parent = plr.PlayerGui

-- ===== КРУГ FOV =====
local fovCircle = Instance.new("Frame")
fovCircle.Name = "FOVCircle"
fovCircle.BackgroundTransparency = 1
fovCircle.BorderSizePixel = 0
fovCircle.AnchorPoint = Vector2.new(0.5, 0.5)
fovCircle.Position = UDim2.new(0.5, 0, 0.5, 0)
fovCircle.Size = UDim2.new(0, 900, 0, 900)
fovCircle.Visible = false
fovCircle.Parent = gui

local circleUI = Instance.new("UICorner")
circleUI.CornerRadius = UDim.new(1, 0)
circleUI.Parent = fovCircle

local circleStroke = Instance.new("UIStroke")
circleStroke.Color = Color3.fromRGB(255, 0, 0)
circleStroke.Thickness = 3
circleStroke.LineJoinMode = Enum.LineJoinMode.Round
circleStroke.ApplyStrokeMode = Enum.ApplyStrokeMode.Border
circleStroke.Parent = fovCircle

-- ===== КНОПКА МЕНЮ (B) =====
local btn = Instance.new("TextButton")
btn.Size = UDim2.new(0, 40, 0, 40)
btn.Position = UDim2.new(0, 50, 0, 50)
btn.BackgroundColor3 = Color3.new(0, 0, 0)
btn.TextColor3 = Color3.new(1, 0, 0)
btn.Text = "SW"
btn.Font = Enum.Font.SourceSansBold
btn.TextSize = 16
btn.BorderSizePixel = 0
btn.Parent = gui

local btnCorner = Instance.new("UICorner")
btnCorner.CornerRadius = UDim.new(0, 16)
btnCorner.Parent = btn

-- ===== МЕНЮ (уменьшенное) =====
local menu = Instance.new("Frame")
menu.Size = UDim2.new(0, 240, 0, 330)
menu.BackgroundColor3 = Color3.new(0, 0, 0)
menu.BorderSizePixel = 2
menu.BorderColor3 = Color3.new(1, 0, 0)
menu.Visible = false
menu.Parent = gui

local menuCorner = Instance.new("UICorner")
menuCorner.CornerRadius = UDim.new(0, 16)
menuCorner.Parent = menu

-- ===== ПЕРЕМЕННЫЕ =====
local aim = false
local esp = false
local wall = false
local hitboxScale = 1.0
local fovSize = 35
local espColor = Color3.new(1, 0, 0)
local fovColor = Color3.new(1, 0, 0)

local highlights = {}
local hitboxObjects = {}

-- ===== WALLHACK =====
local function canSee(pos1, pos2)
    local ray = Ray.new(pos1, pos2 - pos1)
    local part, hit = ws:FindPartOnRayWithIgnoreList(ray, {plr.Character})
    return (hit - pos1).Magnitude >= (pos2 - pos1).Magnitude - 2
end

-- ===== СОЗДАНИЕ КНОПОК (компактные) =====
local function makeToggle(name, y, def, callback)
    local b = Instance.new("TextButton")
    b.Size = UDim2.new(0, 220, 0, 32)
    b.Position = UDim2.new(0, 10, 0, y)
    b.BackgroundColor3 = def and Color3.new(0.3, 0, 0) or Color3.new(0.1, 0.1, 0.1)
    b.TextColor3 = Color3.new(1, 1, 1)
    b.Text = name .. ": " .. (def and "ON" or "OFF")
    b.Font = Enum.Font.SourceSansBold
    b.TextSize = 16
    b.BorderSizePixel = 0
    b.Parent = menu

    local c = Instance.new("UICorner")
    c.CornerRadius = UDim.new(0, 8)
    c.Parent = b

    b.MouseButton1Click:Connect(function()
        def = not def
        b.BackgroundColor3 = def and Color3.new(0.3, 0, 0) or Color3.new(0.1, 0.1, 0.1)
        b.Text = name .. ": " .. (def and "ON" or "OFF")
        if callback then callback(def) end
    end)
    return b
end

-- ===== КНОПКИ (3 шт) =====
makeToggle("Aimbot", 10, aim, function(v) aim = v end)
makeToggle("ESP", 52, esp, function(v) esp = v end)
makeToggle("WallCheck", 94, wall, function(v) wall = v end)

-- ===== ХИТБОКСЫ (категория) =====
local hbLabel = Instance.new("TextLabel")
hbLabel.Size = UDim2.new(0, 220, 0, 20)
hbLabel.Position = UDim2.new(0, 10, 0, 136)
hbLabel.Text = "=== HITBOXES ==="
hbLabel.TextColor3 = Color3.new(1, 0, 0)
hbLabel.BackgroundTransparency = 1
hbLabel.Font = Enum.Font.SourceSansBold
hbLabel.TextSize = 14
hbLabel.Parent = menu

-- Scale (увеличение)
local scaleLabel = Instance.new("TextLabel")
scaleLabel.Size = UDim2.new(0, 100, 0, 20)
scaleLabel.Position = UDim2.new(0, 10, 0, 160)
scaleLabel.Text = "Scale: " .. string.format("%.1f", hitboxScale)
scaleLabel.TextColor3 = Color3.new(1, 1, 1)
scaleLabel.BackgroundTransparency = 1
scaleLabel.Font = Enum.Font.SourceSansBold
scaleLabel.TextSize = 14
scaleLabel.Parent = menu

local scaleBox = Instance.new("TextBox")
scaleBox.Size = UDim2.new(0, 80, 0, 28)
scaleBox.Position = UDim2.new(0, 140, 0, 158)
scaleBox.Text = tostring(hitboxScale)
scaleBox.BackgroundColor3 = Color3.new(0.15, 0, 0)
scaleBox.TextColor3 = Color3.new(1, 1, 1)
scaleBox.Font = Enum.Font.SourceSansBold
scaleBox.TextSize = 16
scaleBox.ClearTextOnFocus = false
scaleBox.Parent = menu

local scaleCorner = Instance.new("UICorner")
scaleCorner.CornerRadius = UDim.new(0, 8)
scaleCorner.Parent = scaleBox

scaleBox.FocusLost:Connect(function()
    local v = tonumber(scaleBox.Text)
    if v then
        hitboxScale = math.clamp(v, 1.0, 5.0)
        scaleLabel.Text = "Scale: " .. string.format("%.1f", hitboxScale)
        scaleBox.Text = string.format("%.1f", hitboxScale)
        -- пересоздать хитбоксы при изменении
        if esp then updateHitboxes() end
    else
        scaleBox.Text = string.format("%.1f", hitboxScale)
    end
end)

-- ===== ЦВЕТ ESP =====
local espColorLabel = Instance.new("TextLabel")
espColorLabel.Size = UDim2.new(0, 80, 0, 20)
espColorLabel.Position = UDim2.new(0, 10, 0, 200)
espColorLabel.Text = "ESP Color"
espColorLabel.TextColor3 = Color3.new(1, 0, 0)
espColorLabel.BackgroundTransparency = 1
espColorLabel.Font = Enum.Font.SourceSansBold
espColorLabel.TextSize = 14
espColorLabel.Parent = menu

local espColorBox = Instance.new("TextBox")
espColorBox.Size = UDim2.new(0, 120, 0, 28)
espColorBox.Position = UDim2.new(0, 100, 0, 198)
espColorBox.Text = "255,0,0"
espColorBox.BackgroundColor3 = Color3.new(0.15, 0, 0)
espColorBox.TextColor3 = Color3.new(1, 1, 1)
espColorBox.Font = Enum.Font.SourceSansBold
espColorBox.TextSize = 16
espColorBox.ClearTextOnFocus = false
espColorBox.Parent = menu

local espCorner = Instance.new("UICorner")
espCorner.CornerRadius = UDim.new(0, 8)
espCorner.Parent = espColorBox

espColorBox.FocusLost:Connect(function()
    local r,g,b = espColorBox.Text:match("(%d+),(%d+),(%d+)")
    if r and g and b then
        r = math.clamp(tonumber(r), 0, 255)
        g = math.clamp(tonumber(g), 0, 255)
        b = math.clamp(tonumber(b), 0, 255)
        espColor = Color3.fromRGB(r, g, b)
        espColorBox.Text = r..","..g..","..b
        for _, h in pairs(highlights) do if h then h.FillColor = espColor end end
        if esp then updateHitboxes() end
    else
        espColorBox.Text = "255,0,0"
        espColor = Color3.new(1,0,0)
    end
end)

-- ===== ЦВЕТ FOV =====
local fovColorLabel = Instance.new("TextLabel")
fovColorLabel.Size = UDim2.new(0, 80, 0, 20)
fovColorLabel.Position = UDim2.new(0, 10, 0, 240)
fovColorLabel.Text = "FOV Color"
fovColorLabel.TextColor3 = Color3.new(1, 0, 0)
fovColorLabel.BackgroundTransparency = 1
fovColorLabel.Font = Enum.Font.SourceSansBold
fovColorLabel.TextSize = 14
fovColorLabel.Parent = menu

local fovColorBox = Instance.new("TextBox")
fovColorBox.Size = UDim2.new(0, 120, 0, 28)
fovColorBox.Position = UDim2.new(0, 100, 0, 238)
fovColorBox.Text = "255,0,0"
fovColorBox.BackgroundColor3 = Color3.new(0.15, 0, 0)
fovColorBox.TextColor3 = Color3.new(1, 1, 1)
fovColorBox.Font = Enum.Font.SourceSansBold
fovColorBox.TextSize = 16
fovColorBox.ClearTextOnFocus = false
fovColorBox.Parent = menu

local fovCorner = Instance.new("UICorner")
fovCorner.CornerRadius = UDim.new(0, 8)
fovCorner.Parent = fovColorBox

fovColorBox.FocusLost:Connect(function()
    local r,g,b = fovColorBox.Text:match("(%d+),(%d+),(%d+)")
    if r and g and b then
        r = math.clamp(tonumber(r), 0, 255)
        g = math.clamp(tonumber(g), 0, 255)
        b = math.clamp(tonumber(b), 0, 255)
        fovColor = Color3.fromRGB(r, g, b)
        circleStroke.Color = fovColor
        fovColorBox.Text = r..","..g..","..b
    else
        fovColorBox.Text = "255,0,0"
        fovColor = Color3.new(1,0,0)
        circleStroke.Color = fovColor
    end
end)

-- ===== FOV РАЗМЕР =====
local fovSizeLabel = Instance.new("TextLabel")
fovSizeLabel.Size = UDim2.new(0, 100, 0, 20)
fovSizeLabel.Position = UDim2.new(0, 10, 0, 280)
fovSizeLabel.Text = "FOV: " .. math.floor(fovSize)
fovSizeLabel.TextColor3 = Color3.new(1, 0, 0)
fovSizeLabel.BackgroundTransparency = 1
fovSizeLabel.Font = Enum.Font.SourceSansBold
fovSizeLabel.TextSize = 14
fovSizeLabel.Parent = menu

local fovBox = Instance.new("TextBox")
fovBox.Size = UDim2.new(0, 80, 0, 28)
fovBox.Position = UDim2.new(0, 140, 0, 278)
fovBox.Text = tostring(math.floor(fovSize))
fovBox.BackgroundColor3 = Color3.new(0.15, 0, 0)
fovBox.TextColor3 = Color3.new(1, 1, 1)
fovBox.Font = Enum.Font.SourceSansBold
fovBox.TextSize = 16
fovBox.ClearTextOnFocus = false
fovBox.Parent = menu

local fovBoxCorner = Instance.new("UICorner")
fovBoxCorner.CornerRadius = UDim.new(0, 8)
fovBoxCorner.Parent = fovBox

fovBox.FocusLost:Connect(function()
    local v = tonumber(fovBox.Text)
    if v then
        fovSize = math.clamp(v, 10, 300)
        fovSizeLabel.Text = "FOV: " .. math.floor(fovSize)
        fovBox.Text = tostring(math.floor(fovSize))
    else
        fovBox.Text = tostring(math.floor(fovSize))
    end
end)

-- ===== ДВИЖЕНИЕ КНОПКИ =====
local dragging = false
local dragStart
local startPos

btn.InputBegan:Connect(function(input)
    if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
        dragging = true
        dragStart = input.Position
        startPos = btn.Position
        uis.MouseIconEnabled = false
    end
end)

btn.InputEnded:Connect(function(input)
    if (input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch) and dragging then
        dragging = false
        uis.MouseIconEnabled = true
    end
end)

uis.InputChanged:Connect(function(input)
    if dragging and (input.UserInputType == Enum.UserInputType.MouseMovement or input.UserInputType == Enum.UserInputType.Touch) then
        local delta = input.Position - dragStart
        btn.Position = UDim2.new(
            startPos.X.Scale, startPos.X.Offset + delta.X,
            startPos.Y.Scale, startPos.Y.Offset + delta.Y
        )
        if menu.Visible then
            local absX = btn.AbsolutePosition.X
            local screenW = cam.ViewportSize.X
            local menuW = menu.AbsoluteSize.X
            if absX < screenW / 2 then
                menu.Position = UDim2.new(0, btn.AbsolutePosition.X + 65, 0, btn.AbsolutePosition.Y)
            else
                menu.Position = UDim2.new(0, btn.AbsolutePosition.X - menuW - 5, 0, btn.AbsolutePosition.Y)
            end
        end
    end
end)

local function toggleMenu()
    menu.Visible = not menu.Visible
    if menu.Visible then
        local absX = btn.AbsolutePosition.X
        local screenW = cam.ViewportSize.X
        local menuW = menu.AbsoluteSize.X
        if absX < screenW / 2 then
            menu.Position = UDim2.new(0, btn.AbsolutePosition.X + 65, 0, btn.AbsolutePosition.Y)
        else
            menu.Position = UDim2.new(0, btn.AbsolutePosition.X - menuW - 5, 0, btn.AbsolutePosition.Y)
        end
    end
end

btn.MouseButton1Click:Connect(toggleMenu)
uis.InputBegan:Connect(function(input)
    if input.KeyCode == Enum.KeyCode.D then toggleMenu() end
end)

-- ===== ХИТБОКСЫ (создание/обновление) =====
local function createHitbox(part, color, scale)
    if not part then return nil end
    local box = Instance.new("BoxHandleAdornment")
    box.Adornee = part
    box.Color3 = color
    box.Transparency = 0.4
    box.ZIndex = 10
    box.AlwaysOnTop = true
    box.Size = part.Size * scale
    box.Parent = part
    return box
end

local function clearHitboxes()
    for _, obj in pairs(hitboxObjects) do
        if obj then obj:Destroy() end
    end
    hitboxObjects = {}
end

local function updateHitboxes()
    clearHitboxes()
    if not esp then return end
    for _, p in ipairs(players:GetPlayers()) do
        if p ~= plr and p.Character then
            local char = p.Character
            local parts = {
                char:FindFirstChild("Head"),
                char:FindFirstChild("UpperTorso"),
                char:FindFirstChild("LowerTorso"),
                char:FindFirstChild("LeftHand"),
                char:FindFirstChild("RightHand"),
                char:FindFirstChild("LeftFoot"),
                char:FindFirstChild("RightFoot")
            }
            for _, part in ipairs(parts) do
                if part then
                    local box = createHitbox(part, espColor, hitboxScale)
                    if box then table.insert(hitboxObjects, box) end
                end
            end
        end
    end
end

-- ===== ГЛАВНЫЙ ЦИКЛ =====
rs.RenderStepped:Connect(function()
    -- ESP (Highlight)
    if esp then
        for _, p in ipairs(players:GetPlayers()) do
            if p ~= plr and p.Character then
                if not highlights[p] then
                    local h = Instance.new("Highlight")
                    h.FillColor = espColor
                    h.FillTransparency = 0.6
                    h.OutlineColor = Color3.new(1, 1, 1)
                    h.OutlineTransparency = 0.2
                    h.DepthMode = Enum.HighlightDepthMode.AlwaysOnTop
                    h.Parent = p.Character
                    highlights[p] = h
                    p.CharacterAdded:Connect(function()
                        if highlights[p] then
                            highlights[p]:Destroy()
                            highlights[p] = nil
                        end
                    end)
                else
                    highlights[p].FillColor = espColor
                end
            end
        end
        for p, h in pairs(highlights) do
            if not p or not p.Character or not h.Parent then
                if h then h:Destroy() end
                highlights[p] = nil
            end
        end
        -- обновить хитбоксы если изменился масштаб или цвет
        updateHitboxes()
    else
        for _, h in pairs(highlights) do
            if h then h:Destroy() end
        end
        highlights = {}
        clearHitboxes()
    end

    -- AIMBOT
    if aim and plr.Character then
        local camPos = cam.CFrame.Position
        local center = Vector2.new(cam.ViewportSize.X / 2, cam.ViewportSize.Y / 2)
        local best = nil
        local bestDist = math.huge

        for _, p in ipairs(players:GetPlayers()) do
            if p ~= plr and p.Character then
                local head = p.Character:FindFirstChild("Head")
                if head then
                    local scr, onScreen = cam:WorldToViewportPoint(head.Position)
                    if onScreen and scr.Z > 0 then
                        local dist2d = (Vector2.new(scr.X, scr.Y) - center).Magnitude
                        if dist2d <= fovSize then
                            local ok = true
                            if wall then ok = canSee(camPos, head.Position) end
                            if ok and dist2d < bestDist then
                                bestDist = dist2d
                                best = head.Position
                            end
                        end
                    end
                end
            end
        end

        if best then
            cam.CFrame = CFrame.lookAt(camPos, best)
        end
    end

    -- КРУГ FOV
    local circleSize = fovSize * 2
    fovCircle.Size = UDim2.new(0, circleSize, 0, circleSize)
    fovCircle.Visible = aim
    fovCircle.Position = UDim2.new(0.5, 0, 0.5, -27)
end)

print("SWILL Light loaded (Hitboxes + ESP Color). Menu: B or D")
