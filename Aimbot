-- ==============================================
-- WINSTIK v1.0 (Гибрид: Дизайн WINSTIK + Логика DENWEN)
-- Размер 200x280. Черное стекло. Идеальный Aimbot & ESP.
-- ==============================================

local player = game.Players.LocalPlayer
local camera = workspace.CurrentCamera
local mouse = player:GetMouse()
local UserInputService = game:GetService("UserInputService")
local RunService = game:GetService("RunService")
local Players = game:GetService("Players")
local Lighting = game:GetService("Lighting")

-- ==============================================
-- ПЕРЕМЕННЫЕ СОСТОЯНИЯ
-- ==============================================
local aimbot = false
local esp = false
local skeleton = true
local fov = 250
local smooth = 0.2
local espColor = Color3.fromRGB(255, 0, 0)
local spin = false
local fog = false
local wallhack = false
local spinSpeed = 5
local fogDensity = 0.5

-- Цвета для выбора (Список цветов)
local colorList = {
    {Color3.fromRGB(255, 0, 0), "КРАСНЫЙ"},
    {Color3.fromRGB(0, 255, 0), "ЗЕЛЁНЫЙ"},
    {Color3.fromRGB(0, 150, 255), "СИНИЙ"},
    {Color3.fromRGB(255, 255, 0), "ЖЁЛТЫЙ"},
    {Color3.fromRGB(255, 0, 255), "ФИОЛЕТ"},
    {Color3.fromRGB(0, 229, 255), "ГОЛУБОЙ"}
}
local colorIndex = 1

-- Переменные для GUI
local FogEffect = nil
local fovCircle = nil

-- ==============================================
-- 1. СОЗДАНИЕ ИНТЕРФЕЙСА (МАЛЕНЬКОЕ, СОВРЕМЕННОЕ)
-- ==============================================
local screenGui = Instance.new("ScreenGui")
screenGui.Name = "WINSTIK"
screenGui.ResetOnSpawn = false
screenGui.Parent = player.PlayerGui

-- Кнопка открытия (B/D)
local openBtn = Instance.new("TextButton")
openBtn.Size = UDim2.new(0, 30, 0, 30)
openBtn.Position = UDim2.new(0, 15, 0, 15)
openBtn.BackgroundColor3 = Color3.fromRGB(8, 8, 8)
openBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
openBtn.Text = "W"
openBtn.Font = Enum.Font.GothamBold
openBtn.TextSize = 14
openBtn.BorderSizePixel = 0
openBtn.Parent = screenGui

local btnCorner = Instance.new("UICorner")
btnCorner.CornerRadius = UDim.new(0, 8)
btnCorner.Parent = openBtn

local btnStroke = Instance.new("UIStroke")
btnStroke.Color = espColor
btnStroke.Thickness = 1.5
btnStroke.Transparency = 0.7
btnStroke.Parent = openBtn

-- Главное меню (200x280)
local mainFrame = Instance.new("Frame")
mainFrame.Size = UDim2.new(0, 200, 0, 280)
mainFrame.Position = UDim2.new(0.5, -100, 0.5, -140)
mainFrame.BackgroundColor3 = Color3.fromRGB(8, 8, 8)
mainFrame.BackgroundTransparency = 0.15
mainFrame.BorderSizePixel = 1
mainFrame.BorderColor3 = espColor
mainFrame.Active = true
mainFrame.Draggable = true
mainFrame.Visible = false
mainFrame.Parent = screenGui

local menuCorner = Instance.new("UICorner")
menuCorner.CornerRadius = UDim.new(0, 10)
menuCorner.Parent = mainFrame

-- Заголовок
local title = Instance.new("TextLabel")
title.Size = UDim2.new(1, 0, 0, 30)
title.Position = UDim2.new(0, 0, 0, 0)
title.BackgroundTransparency = 1
title.Text = "WINSTIK 1.0"
title.TextColor3 = espColor
title.TextScaled = true
title.Font = Enum.Font.GothamBold
title.Parent = mainFrame

-- Кнопка закрытия (Х)
local closeBtn = Instance.new("TextButton")
closeBtn.Size = UDim2.new(0, 20, 0, 20)
closeBtn.Position = UDim2.new(1, -25, 0, 5)
closeBtn.BackgroundTransparency = 1
closeBtn.Text = "✕"
closeBtn.TextColor3 = espColor
closeBtn.TextScaled = true
closeBtn.Font = Enum.Font.GothamBold
closeBtn.Parent = mainFrame
closeBtn.MouseButton1Click:Connect(function()
    screenGui:Destroy()
end)

-- Линия разделителя
local line = Instance.new("Frame")
line.Size = UDim2.new(0.9, 0, 0, 1)
line.Position = UDim2.new(0.05, 0, 0, 35)
line.BackgroundColor3 = espColor
line.BorderSizePixel = 0
line.Parent = mainFrame

-- ==============================================
-- 2. ВСПОМОГАТЕЛЬНЫЕ ФУНКЦИИ UI
-- ==============================================

-- Функция для создания карточки с тумблером
local function createToggle(text, yPos)
    local frame = Instance.new("Frame")
    frame.Size = UDim2.new(0.9, 0, 0, 28)
    frame.Position = UDim2.new(0.05, 0, 0, yPos)
    frame.BackgroundColor3 = Color3.fromRGB(20, 20, 20)
    frame.BackgroundTransparency = 0.1
    frame.BorderSizePixel = 1
    frame.BorderColor3 = Color3.fromRGB(255, 255, 255)
    frame.BorderColor3 = frame.BorderColor3:Lerp(Color3.fromRGB(0, 0, 0), 0.9)
    frame.Parent = mainFrame

    local frameCorner = Instance.new("UICorner")
    frameCorner.CornerRadius = UDim.new(0, 6)
    frameCorner.Parent = frame

    local label = Instance.new("TextLabel")
    label.Size = UDim2.new(0.6, 0, 1, 0)
    label.Position = UDim2.new(0.05, 0, 0, 0)
    label.BackgroundTransparency = 1
    label.Text = text
    label.TextColor3 = Color3.fromRGB(220, 220, 220)
    label.TextXAlignment = Enum.TextXAlignment.Left
    label.TextScaled = true
    label.Font = Enum.Font.Gotham
    label.Parent = frame

    local btn = Instance.new("TextButton")
    btn.Size = UDim2.new(0, 40, 0, 18)
    btn.Position = UDim2.new(0.75, 0, 0.5, -9)
    btn.BackgroundColor3 = Color3.fromRGB(50, 50, 50)
    btn.Text = "OFF"
    btn.TextColor3 = Color3.fromRGB(255, 255, 255)
    btn.TextScaled = true
    btn.Font = Enum.Font.GothamBold
    btn.Parent = frame

    local btnCorner = Instance.new("UICorner")
    btnCorner.CornerRadius = UDim.new(1, 0)
    btnCorner.Parent = btn

    return frame, btn
end

-- Функция для создания ползунка (с бегунком)
local function createSlider(text, yPos, minVal, maxVal, onChange)
    local frame = Instance.new("Frame")
    frame.Size = UDim2.new(0.9, 0, 0, 40)
    frame.Position = UDim2.new(0.05, 0, 0, yPos)
    frame.BackgroundColor3 = Color3.fromRGB(20, 20, 20)
    frame.BackgroundTransparency = 0.1
    frame.BorderSizePixel = 1
    frame.BorderColor3 = Color3.fromRGB(255, 255, 255)
    frame.BorderColor3 = frame.BorderColor3:Lerp(Color3.fromRGB(0, 0, 0), 0.9)
    frame.Parent = mainFrame

    local frameCorner = Instance.new("UICorner")
    frameCorner.CornerRadius = UDim.new(0, 6)
    frameCorner.Parent = frame

    local label = Instance.new("TextLabel")
    label.Size = UDim2.new(0.5, 0, 0.5, 0)
    label.Position = UDim2.new(0.05, 0, 0, 0)
    label.BackgroundTransparency = 1
    label.Text = text
    label.TextColor3 = Color3.fromRGB(200, 200, 200)
    label.TextXAlignment = Enum.TextXAlignment.Left
    label.TextScaled = true
    label.Font = Enum.Font.Gotham
    label.Parent = frame

    local valueLabel = Instance.new("TextLabel")
    valueLabel.Size = UDim2.new(0.3, 0, 0.5, 0)
    valueLabel.Position = UDim2.new(0.65, 0, 0, 0)
    valueLabel.BackgroundTransparency = 1
    valueLabel.Text = tostring(minVal)
    valueLabel.TextColor3 = espColor
    valueLabel.TextXAlignment = Enum.TextXAlignment.Right
    valueLabel.TextScaled = true
    valueLabel.Font = Enum.Font.Gotham
    valueLabel.Parent = frame

    -- Полоса ползунка
    local sliderBg = Instance.new("Frame")
    sliderBg.Size = UDim2.new(0.9, 0, 0, 4)
    sliderBg.Position = UDim2.new(0.05, 0, 0.7, 0)
    sliderBg.BackgroundColor3 = Color3.fromRGB(50, 50, 50)
    sliderBg.BorderSizePixel = 0
    sliderBg.Parent = frame

    local fill = Instance.new("Frame")
    fill.Size = UDim2.new(0, 0, 1, 0)
    fill.BackgroundColor3 = espColor
    fill.BorderSizePixel = 0
    fill.Parent = sliderBg

    local knob = Instance.new("Frame")
    knob.Size = UDim2.new(0, 10, 0, 10)
    knob.Position = UDim2.new(0, -5, 0.5, -5)
    knob.BackgroundColor3 = espColor
    knob.BorderSizePixel = 0
    knob.Parent = sliderBg

    -- Логика перетаскивания
    local dragging = false
    local function updateSlider(input)
        if not dragging then return end
        local pos = math.clamp((input.Position.X - sliderBg.AbsolutePosition.X) / sliderBg.AbsoluteSize.X, 0, 1)
        local value = minVal + (maxVal - minVal) * pos
        valueLabel.Text = tostring(math.floor(value))
        fill.Size = UDim2.new(pos, 0, 1, 0)
        knob.Position = UDim2.new(pos, -5, 0.5, -5)
        onChange(value)
    end

    sliderBg.InputBegan:Connect(function(input)
        if input.UserInputType == Enum.UserInputType.MouseButton1 then
            dragging = true
            updateSlider(input)
        end
    end)
    sliderBg.InputEnded:Connect(function(input)
        if input.UserInputType == Enum.UserInputType.MouseButton1 then
            dragging = false
        end
    end)
    sliderBg.InputChanged:Connect(function(input)
        if input.UserInputType == Enum.UserInputType.MouseMovement then
            updateSlider(input)
        end
    end)

    return frame
end

-- ==============================================
-- 3. СОЗДАНИЕ ЭЛЕМЕНТОВ МЕНЮ
-- ==============================================

-- Основные тумблеры
local _, aimBtn = createToggle("AIMBOT", 45)
local _, espBtn = createToggle("ESP", 78)
local _, wallBtn = createToggle("WALLHACK", 111)
local _, spinBtn = createToggle("SPIN 360", 144)
local _, fogBtn = createToggle("FOG", 177)

-- Кнопка выбора цвета
local colorFrame = Instance.new("Frame")
colorFrame.Size = UDim2.new(0.9, 0, 0, 28)
colorFrame.Position = UDim2.new(0.05, 0, 0, 210)
colorFrame.BackgroundColor3 = Color3.fromRGB(20, 20, 20)
colorFrame.BackgroundTransparency = 0.1
colorFrame.BorderSizePixel = 1
colorFrame.BorderColor3 = Color3.fromRGB(255, 255, 255)
colorFrame.BorderColor3 = frame.BorderColor3:Lerp(Color3.fromRGB(0, 0, 0), 0.9)
colorFrame.Parent = mainFrame

local colorCorner = Instance.new("UICorner")
colorCorner.CornerRadius = UDim.new(0, 6)
colorCorner.Parent = colorFrame

local colorLabel = Instance.new("TextLabel")
colorLabel.Size = UDim2.new(0.6, 0, 1, 0)
colorLabel.Position = UDim2.new(0.05, 0, 0, 0)
colorLabel.BackgroundTransparency = 1
colorLabel.Text = "ЦВЕТ"
colorLabel.TextColor3 = Color3.fromRGB(220, 220, 220)
colorLabel.TextXAlignment = Enum.TextXAlignment.Left
colorLabel.TextScaled = true
colorLabel.Font = Enum.Font.Gotham
colorLabel.Parent = colorFrame

local colorBtn = Instance.new("TextButton")
colorBtn.Size = UDim2.new(0, 70, 0, 20)
colorBtn.Position = UDim2.new(0.65, 0, 0.5, -10)
colorBtn.BackgroundColor3 = Color3.fromRGB(50, 50, 50)
colorBtn.Text = "КРАСНЫЙ"
colorBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
colorBtn.TextScaled = true
colorBtn.Font = Enum.Font.GothamBold
colorBtn.Parent = colorFrame

colorBtn.MouseButton1Click:Connect(function()
    colorIndex = colorIndex % #colorList + 1
    espColor = colorList[colorIndex][1]
    colorBtn.Text = colorList[colorIndex][2]
    -- Обновляем цвет элементов меню
    title.TextColor3 = espColor
    line.BackgroundColor3 = espColor
    btnStroke.Color = espColor
    -- Обновляем цвет всех слайдеров
    for _, child in ipairs(mainFrame:GetDescendants()) do
        if child:IsA("Frame") and child.Size.Y.Offset == 4 then
            local fill = child:FindFirstChild("Frame")
            if fill then
                fill.BackgroundColor3 = espColor
            end
        end
    end
end)

-- ==============================================
-- 4. ЛОГИКА КНОПОК И ПОЛЗУНКОВ
-- ==============================================

-- Логика тумблеров
aimBtn.MouseButton1Click:Connect(function()
    aimbot = not aimbot
    aimBtn.Text = aimbot and "ON" or "OFF"
    aimBtn.BackgroundColor3 = aimbot and espColor or Color3.fromRGB(50, 50, 50)
end)

espBtn.MouseButton1Click:Connect(function()
    esp = not esp
    espBtn.Text = esp and "ON" or "OFF"
    espBtn.BackgroundColor3 = esp and espColor or Color3.fromRGB(50, 50, 50)
end)

wallBtn.MouseButton1Click:Connect(function()
    wallhack = not wallhack
    wallBtn.Text = wallhack and "ON" or "OFF"
    wallBtn.BackgroundColor3 = wallhack and espColor or Color3.fromRGB(50, 50, 50)
end)

spinBtn.MouseButton1Click:Connect(function()
    spin = not spin
    spinBtn.Text = spin and "ON" or "OFF"
    spinBtn.BackgroundColor3 = spin and espColor or Color3.fromRGB(50, 50, 50)
end)

fogBtn.MouseButton1Click:Connect(function()
    fog = not fog
    fogBtn.Text = fog and "ON" or "OFF"
    fogBtn.BackgroundColor3 = fog and espColor or Color3.fromRGB(50, 50, 50)
end)

-- Ползунки FOV, SMOOTH, SPIN SPEED
createSlider("FOV", 240, 100, 500, function(v) fov = v end)
-- Следующие ползунки будут добавлены позже, если нужно расширить меню

-- ==============================================
-- 5. КРУГ FOV (Аналогично DENWEN)
-- ==============================================
local fovCircle = Instance.new("CircleHandleAdornment")
fovCircle.Radius = fov / 3
fovCircle.Color3 = espColor
fovCircle.Transparency = 0.4
fovCircle.AlwaysOnTop = true
fovCircle.Parent = camera

RunService.RenderStepped:Connect(function()
    fovCircle.Radius = fov / 3
    fovCircle.CFrame = camera.CFrame
end)

-- ==============================================
-- 6. AIMBOT (Точь-в-точь из DENWEN)
-- ==============================================
local function getClosestPlayer()
    local closest, shortest = nil, fov
    for _, plr in ipairs(Players:GetPlayers()) do
        if plr ~= player and plr.Character and plr.Character:FindFirstChild("Humanoid") and plr.Character.Humanoid.Health > 0 then
            local part = plr.Character:FindFirstChild("Head")
            if part then
                local pos, onScreen = camera:WorldToViewportPoint(part.Position)
                if onScreen then
                    local dist = (Vector2.new(pos.X, pos.Y) - Vector2.new(mouse.X, mouse.Y)).Magnitude
                    if dist < shortest then
                        shortest = dist
                        closest = plr
                    end
                end
            end
        end
    end
    return closest
end

RunService.RenderStepped:Connect(function()
    if not aimbot then return end
    local target = getClosestPlayer()
    if target then
        local aimPart = target.Character:FindFirstChild("Head")
        if aimPart then
            local aimPos = aimPart.Position
            local currentPos = camera.CFrame.Position
            local direction = (aimPos - currentPos).Unit
            camera.CFrame = camera.CFrame:Lerp(CFrame.new(currentPos, currentPos + direction), smooth)
        end
    end
end)

-- ==============================================
-- 7. ESP (Точь-в-точь из DENWEN + Wallhack)
-- ==============================================
local espObjects = {}

local function createESP(plr)
    if espObjects[plr] or not plr.Character then return end
    local char = plr.Character
    local folder = Instance.new("Folder")
    folder.Name = "WINSTIK_ESP"
    folder.Parent = char

    local box = Instance.new("BoxHandleAdornment")
    box.Size = Vector3.new(3, 5, 1)
    box.Adornee = char
    box.Color3 = espColor
    box.Transparency = 0.2
    box.AlwaysOnTop = wallhack
    box.Parent = folder

    if skeleton then
        local function addLine(p1, p2)
            local line = Instance.new("LineHandleAdornment")
            line.Adornee = char
            line.Point1 = p1.Position
            line.Point2 = p2.Position
            line.Color3 = espColor
            line.Thickness = 1.5
            line.Transparency = 0.3
            line.AlwaysOnTop = wallhack
            line.Parent = folder
        end
        local head = char:FindFirstChild("Head")
        local torso = char:FindFirstChild("UpperTorso") or char:FindFirstChild("Torso")
        local la = char:FindFirstChild("Left Arm") or char:FindFirstChild("LeftUpperArm")
        local ra = char:FindFirstChild("Right Arm") or char:FindFirstChild("RightUpperArm")
        local ll = char:FindFirstChild("Left Leg") or char:FindFirstChild("LeftLowerLeg")
        local rl = char:FindFirstChild("Right Leg") or char:FindFirstChild("RightLowerLeg")
        if head and torso then
            addLine(head, torso)
            if la then addLine(torso, la) end
            if ra then addLine(torso, ra) end
            if ll then addLine(torso, ll) end
            if rl then addLine(torso, rl) end
        end
    end

    local nameGui = Instance.new("BillboardGui")
    nameGui.Adornee = char:FindFirstChild("Head")
    nameGui.Size = UDim2.new(0, 100, 0, 20)
    nameGui.StudsOffset = Vector3.new(0, 2.5, 0)
    nameGui.Parent = folder
    local nameLabel = Instance.new("TextLabel")
    nameLabel.Size = UDim2.new(1, 0, 1, 0)
    nameLabel.BackgroundTransparency = 1
    nameLabel.Text = plr.Name
    nameLabel.TextColor3 = Color3.fromRGB(255, 255, 255)
    nameLabel.TextScaled = true
    nameLabel.Font = Enum.Font.GothamBold
    nameLabel.Parent = nameGui

    local healthGui = Instance.new("BillboardGui")
    healthGui.Adornee = char:FindFirstChild("Head")
    healthGui.Size = UDim2.new(0, 50, 0, 4)
    healthGui.StudsOffset = Vector3.new(0, 2, 0)
    healthGui.Parent = folder
    local bg = Instance.new("Frame")
    bg.Size = UDim2.new(1, 0, 1, 0)
    bg.BackgroundColor3 = Color3.fromRGB(50, 50, 50)
    bg.Parent = healthGui
    local fill = Instance.new("Frame")
    fill.BackgroundColor3 = Color3.fromRGB(0, 200, 0)
    fill.Size = UDim2.new(1, 0, 1, 0)
    fill.Parent = bg

    espObjects[plr] = {Folder = folder, Health = fill}
end

RunService.RenderStepped:Connect(function()
    if esp then
        for _, plr in ipairs(Players:GetPlayers()) do
            if plr ~= player and plr.Character and not espObjects[plr] then
                createESP(plr)
            end
        end
        for plr, data in pairs(espObjects) do
            if not plr.Character or not plr.Character.Parent then
                data.Folder:Destroy()
                espObjects[plr] = nil
            else
                local hum = plr.Character:FindFirstChild("Humanoid")
                if hum then
                    data.Health.Size = UDim2.new(hum.Health / hum.MaxHealth, 0, 1, 0)
                end
            end
        end
    else
        for _, data in pairs(espObjects) do
            data.Folder:Destroy()
        end
        espObjects = {}
    end
end)

-- ==============================================
-- 8. ДОБАВЛЕННЫЕ ФУНКЦИИ: SPIN и FOG
-- ==============================================

-- SPIN 360
RunService.RenderStepped:Connect(function()
    if spin and player.Character then
        local hrp = player.Character:FindFirstChild("HumanoidRootPart")
        if hrp then
            hrp.CFrame = hrp.CFrame * CFrame.Angles(0, math.rad(spinSpeed), 0)
        end
    end
end)

-- FOG
RunService.RenderStepped:Connect(function()
    if fog then
        if not FogEffect then
            FogEffect = Instance.new("Atmosphere")
            FogEffect.Name = "WINSTIK_Fog"
            FogEffect.Color = Color3.fromRGB(255, 255, 255)
            FogEffect.Density = fogDensity
            FogEffect.Parent = Lighting
        end
    else
        if FogEffect then
            FogEffect:Destroy()
            FogEffect = nil
        end
    end
end)

-- ==============================================
-- 9. ОТКРЫТИЕ И ЗАКРЫТИЕ (B или D)
-- ==============================================
openBtn.MouseButton1Click:Connect(function()
    mainFrame.Visible = not mainFrame.Visible
end)

UserInputService.InputBegan:Connect(function(input)
    if input.KeyCode == Enum.KeyCode.B or input.KeyCode == Enum.KeyCode.D then
        mainFrame.Visible = not mainFrame.Visible
    end
end)

print("WINSTIK v1.0 загружен! Нажми B или D для меню.")
